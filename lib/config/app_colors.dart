import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color backgroundPrimary = Color(0xFFE8F5E9);

  static const Color primaryGreen = Color(0xFF4CAF50);

  static const Color dateTimeGreen = Color(0xFF277c5b);

  // Cards
  static const Color cardBackground = Colors.white;

  static const Color replacementChipBg = Color(0xFFfdf7eb);
  static const Color replacementChipText = Color(0xFFd29434);

  // Status chips - Renewal
  static const Color renewalChipBg = Color(0xFFE3F2FD);
  static const Color renewalChipText = Color(0xFF2196F3);

  // Status chips - Grace Period
  static const Color gracePeriodChipBg = Color(0xFFE8FDD8);
  static const Color gracePeriodChipText = Color(0xFF64DD17);

  // Location link
  static const Color locationBlue = Color(0xFF2196F3);

  // Reject button & destructive (image: dark red)
  static const Color rejectRed = Color(0xFFF44336);

  // Divider under date
  static const Color dividerColor = Color(0xDDF3EFEF);

  // Body text (labels, Booking ID - image dark gray)
  static const Color textBody = Color(0xFF333333);
  static const Color titleColor = Color(0xFF7c7c7c);

  // Booking ID line: label "Booking ID: " (medium grey) and value "#292" (dark grey)
  static const Color bookingIdLabel = Color(0xFF6C7176);
  static const Color bookingIdValue = Color(0xFF2C3136);

  // Bottom nav
  static const Color bottomNavBackground = Color(0xFF212121);
  static const Color bottomNavSelectedPill = Color(0xFFDCEDC8);
  static const Color bottomNavUnselected = Color(0xFF9E9E9E);

  // Text
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Button (for AppButtonStyles - image: light fill + dark text/border)
  static const Color acceptButtonBg = Color(0xFFe5f1ed);
  static const Color acceptButtonFg = Color(0xFF20694c);
  static const Color rejectButtonBg = Color(0xFFFFEBEE);
  static const Color rejectButtonFg = Color(0xFFD32F2F);
}
